import ContactUserCard from "./ContactUserCard"

const Contacts = () => {
  return (
    <div className="Contacts">
      <h2>Contacts</h2>
      <ContactUserCard />
    </div>
  )
}

export default Contacts

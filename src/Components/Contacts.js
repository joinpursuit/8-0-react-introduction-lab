import react from "react";

const Contacts = () => {
    return (
        <div className="contacts">
            <strong>
                Contacts
            </strong>
        </div>
    );
};

export default Contacts;
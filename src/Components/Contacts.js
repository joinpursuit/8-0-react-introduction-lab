import ContactUserCard from "./ContactUserCard";

function Contacts() {
  return (
    <section className="contacts">
      <h1>Contacts</h1>
      <ContactUserCard />
      <ContactUserCard />
      <ContactUserCard />
      <ContactUserCard />
    </section>
  );
}

export default Contacts;

import "./NavBar.css";

const NavBar = () => {
  return (
    <nav className="navBar">
      <button className="navButton">Sign in</button>
      <button className="navButton">Create an account</button>
      <button className="navButton">What is Pursuit?</button>
    </nav>
  );
};

export default NavBar;

import "./NavBar.css";

const NavBar = () => {
  return (
    <header className="header">
      <button id="pursuitButton">What is Pursuit?</button>
      <button id="accountButton">Create an Account</button>
      <button id="signInButton">Sign in</button>
    </header>
  );
};

export default NavBar;
